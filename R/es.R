es <- function(...){
    warning("Unfortunately es() function is no longer available in \"TStools\". It is now part of \"smooth\" package. \nInstalling 'smooth' is easy - just type install.packages(\"smooth\") and you are done!",call.=FALSE);
}
sim.ets <- function(...){
    warning("Unfortunately sim.ets() function is no longer available in \"TStools\". It is now part of \"smooth\" package. \nInstalling 'smooth' is easy - just type install.packages(\"smooth\") and you are done!",call.=FALSE);
}